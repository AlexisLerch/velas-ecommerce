generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model Product {
  id          String   @id @default(uuid())
  name        String
  price       Int
  description String?
  image       String
  slug        String   @unique
  stock       Int
  createdAt   DateTime @default(now())
}